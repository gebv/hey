
language: go

go:
  - 1.5.2

addons:
  postgresql: "9.4"

services:
  - postgresql

install: make vendor_get

before_script:
  - psql -c 'create database hey;' -U postgres
  - psql -f schema/v0.1.sql -U postgres hey
  - cp config/config.json.travis config/config.json
  - 

script:
  - make test